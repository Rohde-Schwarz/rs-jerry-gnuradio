# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-msr4
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

set(ADDITIONAL_FLAGS "-Wall -Wextra -Werror -Wno-unknown-pragmas")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${ADDITIONAL_FLAGS}")
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${ADDITIONAL_FLAGS}")

list(APPEND msr4_sources
    rs_jerry_impl.cc
)

set(msr4_sources "${msr4_sources}" PARENT_SCOPE)
if(NOT msr4_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT msr4_sources)

add_library(gnuradio-msr4 SHARED ${msr4_sources})

include_directories(/usr/local/include/iqClient/generated/)
find_library(libjerryDriver libjerryDriver.a /usr/local/lib/)

set(LOCAL_GRPC_PATH /usr)
set(ENV{PKG_CONFIG_PATH} ${LOCAL_GRPC_PATH}/share/pkgconfig:${LOCAL_GRPC_PATH}/lib/pkgconfig:$ENV{PKG_CONFIG_PATH})
set(CMAKE_INCLUDE_PATH ${LOCAL_GRPC_PATH}/include ${CMAKE_INCLUDE_PATH})
set(CMAKE_LIBRARY_PATH ${LOCAL_GRPC_PATH}/lib ${CMAKE_LIBRARY_PATH})
list(APPEND CMAKE_PREFIX_PATH ${LOCAL_GRPC_PATH})

find_package(Protobuf REQUIRED)
find_package(gRPC CONFIG REQUIRED)
find_package(PkgConfig REQUIRED)
pkg_check_modules(DPDK REQUIRED libdpdk)
find_package(jsoncpp REQUIRED)

target_link_libraries(gnuradio-msr4 gnuradio::gnuradio-runtime ${libjerryDriver} gRPC::grpc++ ${DPDK_LIBRARIES} jsoncpp)
target_include_directories(gnuradio-msr4
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-msr4 PROPERTIES DEFINE_SYMBOL "gnuradio_msr4_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-msr4 PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-msr4)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_msr4_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-msr4)

if(NOT test_msr4_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_msr4_sources)

foreach(qa_file ${test_msr4_sources})
    GR_ADD_CPP_TEST("msr4_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
